<h1> Your Rooms </h1>

<ul>
  <% rooms.forEach((oneRoom) => { %>
    <li>
      <h2> <%= oneRoom.name %> </h2>
      <p> <%= oneRoom.description %> </p>
      <img src="<%= oneRoom.picture %>">
    </li>
  <% }) %>
</ul>
